// Ambient types. Astro's generated `.astro/types.d.ts` (referenced in tsconfig) provides
// astro/client types; this just declares the Fontsource side-effect import, which injects
// @font-face CSS and ships no type declarations of its own.
declare module '@fontsource-variable/outfit';
